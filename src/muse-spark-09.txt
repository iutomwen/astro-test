Act as a Principal Frontend Engineer specializing in multi-tenant family relationship dashboards, secure role-based queries, and accessible data visualizations.

Generate the Parent Dashboard Portal View (`app/dashboard/parent/page.tsx`), the student detail view (`app/dashboard/parent/student/[id]/page.tsx`), and its accompanying Vinext edge API route handler (`app/api/parent/student/data/route.ts`). This module allows authenticated users with the 'parent' role to view an aggregate summary of their linked students, inspect specific assignment grades calculated by the AI engine, check overdue deadlines, and review school billings.

Do not truncate code blocks, do not leave empty placeholder state loops, and implement full structural support for light and dark modes.

### 1. RELATIONSHIP DATA GRID & SUMMARY PANEL (`/parent/page.tsx`)
Create a comprehensive family overview screen containing:
- Multi-Student Selector: A sleek horizontal carousel or button group using Shadcn Avatars to toggle between different linked children if a parent has multiple students registered on the platform.
- High-Level Performance Metrics: A grid of 3 responsive KPI cards detailing: Cumulative GPA/Average Grade, Total Assignments Completed vs Outstanding, and a clean Alert Banner counting the absolute number of Urgent Deadlines within the next 48 hours.

### 2. STUDENT DETAIL WORKSPACE (`/student/[id]/page.tsx`)
Create a deep-dive performance dashboard for the selected child:
- Upcoming Deadlines Track: A vertical list displaying upcoming homework tasks generated by the AI Homework Builder, featuring absolute relative time counters and color-coded priority flags (Red for overdue/urgent, Amber for this week, Green for safe).
- AI Grade History Ledger: A detailed feed displaying rows of completed tasks retrieved from the 'homework_submissions' table. Clicking a row must toggle a smooth Shadcn Dialog window that reveals the exact detailed JSON output generated by the AI Grading Engine—specifically showing totalScoreAwarded, maxPossibleScore, and the full markdown feedback summary string.

### 3. VINEXT EDGE ROUTE RELATIONSHIP HANDLER
In the backend API handler file, enforce rigorous edge authorization safeguards:
- Environment & ORM Context: Use exactly: `import { env } from "cloudflare:workers";` and instantiate Drizzle via `const db = drizzle(env.DB);`.
- Strict Family Guard Check: Intercept the better-auth edge session context. Verify that the logged-in user's role is strictly 'parent'. Query a relational connection table (e.g., 'parent_student_relations') to explicitly verify that the parent user is legally authorized to view data for the requested student ID. Return an absolute 403 Forbidden JSON status immediately if this check fails.
- Aggregated Data Payload: Query Cloudflare D1 efficiently using Drizzle join operations. Return a structured JSON block combining the target student's profile information, an array of active homework schedules, their historical graded answer indices, and basic tenant tuition payment statuses.

### 4. PRESENTATION STYLING & RESPONSIVENESS
- Accessible Mobile Layouts: Ensure complex data matrices degrade into simple stacked layouts on mobile screens to allow easy reading on the move.
- Deep Dark Mode Coordination: Style all card borders, interactive lists, dialogue boxes, and text fields using semantic utility markers (`bg-background`, `border-border`, `text-card-foreground`, `dark:bg-zinc-900`) that respond seamlessly to system-wide theme switches.
