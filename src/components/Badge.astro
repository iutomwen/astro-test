---
interface Props {
  label: string;
  color?: string;
}

const { label, color = "gray" } = Astro.props;
---

<span class={`inline-block px-2 py-0.5 rounded text-white bg-${color}-600 text-sm`}>
  {label}
</span>