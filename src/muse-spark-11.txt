Act as a Principal Frontend Engineer specializing in queue management architectures, rapid triage control layouts, and real-time support data feeds.

Generate the Customer Service Help Desk View (`app/dashboard/support/tickets/page.tsx`), the individual ticket detail and resolution console (`app/dashboard/support/tickets/[id]/page.tsx`), and its accompanying Vinext edge API route handler (`app/api/support/tickets/manage/route.ts`). This module allows authenticated users with the 'customerService' role to browse, assign, filter, and close system tickets.

Do not truncate code blocks, do not leave empty placeholder loops, and implement full structural support for light and dark modes.

### 1. SUPPORT QUEUE DASHBOARD (`/tickets/page.tsx`)
Create a high-velocity triage grid containing:
- Account Queue Filters: Faceted filtering menus using Shadcn UI dropdowns to narrow the queue by: Ticket Status ('Open' | 'Pending' | 'Resolved'), Urgency ('Low' | 'Medium' | 'High' | 'Critical').
- Ticket Entry Row Display: Map rows of incoming tickets featuring:
  * Unified Submitter Info: Displays user avatar fallback icon, display name, and clear role badge ('student', 'parent', or 'tutor').
  * Subject & Reference: Highlighting the issue category (e.g., "AI Grading Dispute", "Login Failure", "Billing Discrepancy") with a clean, unique alphanumeric tracking token.
  * Time-elapsed Indicator: Visual relative timestamp tracking minutes since submission to ensure SLA compliance metrics are hit.

### 2. RESOLUTION CONSOLE (`/tickets/[id]/page.tsx`)
Create a dual-pane workspace optimized for fast ticket closure:
- Left Pane (Context & Audit Trail): Displays the comprehensive issue description, parent details, and a dynamic Audit Widget. If the ticket category matches an "AI Grading Dispute", automatically embed a diagnostic view showing the raw underlying scores and feedback summary string outputted by the Automated AI Grading Engine directly from the database.
- Right Pane (Communication Log & Actions): A vertical timeline showing chronological thread messages. Include a rich response form containing a Shadcn Textarea for typing replies, a quick dropdown to insert macro-responses, and a primary footer housing a "Send & Resolve Ticket" action button.

### 3. VINEXT EDGE ROUTE QUEUE HANDLER
In the backend API handler file, enforce rigorous edge authorization safeguards:
- Runtime Environment Setup: Use exactly: `import { env } from "cloudflare:workers";` and instantiate Drizzle via `const db = drizzle(env.DB);`.
- Access Control Verification: Validate the incoming better-auth edge session context. Confirm that the authenticated user's role is strictly registered as 'customerService' or 'admin'. Return an absolute 403 Forbidden status immediately if validation checks fail.
- Atomic SQL Transaction: Process incoming PATCH updates carrying `ticketId` (string), `assignedAgentId` (string), `status` (string), and optional resolution notes. Write these changes down into the D1 `support_tickets` table via Drizzle ORM, automatically appending a fresh compilation timestamp tracking flag.

### 4. PRESENTATION STYLING & RESPONSIVENESS
- High-Density Responsive Layouts: Optimize the split views to preserve spacing across dense text strings on standard screens while dropping smoothly into a single-pane vertical view on mobile displays.
- Full Dark Mode Coordination: Style all canvas backdrops, dialog elements, input states, and row selection boundaries using semantic utility tokens (`bg-background`, `border-border`, `text-card-foreground`, `dark:bg-zinc-950`) responding seamlessly to system theme switches.
